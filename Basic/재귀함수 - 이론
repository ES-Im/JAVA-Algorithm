// 재귀함수(자기자신을 호출하는 함수)
// : 자연수 N이 입력되면 재귀함수를 이용하여 1부터 N까지 출력하는 프로그램을 작성

public class Main {

    static void DFS(int n) {    // if ... else 활용!!
        if(n == 0) return;
        System.out.println(n);
        DFS(n-1);
    }

    public static void main(String[] args) {
        DFS(3);
    }
}
